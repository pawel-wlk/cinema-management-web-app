Cinema management app made using express.js and MariaDB.

To run the app on your machine, you must have docker and docker-compose installed.

Then, simply run:
```
docker-compose build
```
and
```
docker-compose up
```

